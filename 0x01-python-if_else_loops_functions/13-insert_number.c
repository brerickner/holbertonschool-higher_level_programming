#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 * listint_t *insert_node- listint_t **head, int number
 * @head: pointer to head of list
 * @number: number in nodes of list
 * Return: address of new node. Else NULL
 */
listint_t *insert_node(listint_t **head, int number)
{
	(void)number;

	return (*head);
}
